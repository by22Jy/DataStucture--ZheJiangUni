树的引入：
对于一个查找问题，顺序查找o(n)时间复杂度，效率低。
通过二分查找变成o(logn)时间复杂度，效率高。
二分查找 可看作每次取可能中点的一棵树，每次取一半，直到找到目标值。
通过树的数据结构 优化查找效率。

通过儿子-兄弟表示法，可以用一棵树表示多叉树。 已知n个节点则一共有2n个位置，n-1个非零元素，n个节点，n-1个指针，2n-(n-1)=n+1个空指针。
如果把这棵树倾斜来看，便是一颗二叉树。